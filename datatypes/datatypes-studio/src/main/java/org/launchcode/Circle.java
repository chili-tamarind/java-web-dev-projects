package org.launchcode;

public class Circle {
    public static double getArea(double numRadius) {
        double numArea = Math.PI * Math.pow(numRadius, 2); // Raise to power
        return numArea;
    }
}